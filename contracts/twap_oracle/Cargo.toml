[package]
name = "soromint-twap-oracle"
version = "1.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "22.0.0"

[dev-dependencies]
soroban-sdk = { version = "22.0.0", features = ["testutils"] }
soromint-amm-pool = { path = "../amm_pool" }
soromint-token = { path = "../token" }
